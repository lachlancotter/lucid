# Render Columns

Add columns to the board so the kanban structure is visible before any cards or
mutating workflows are introduced.

## Goal

Render the default board columns.

## Outline

- Introduce the columns feature
- Define the default `Todo`, `Doing`, and `Done` columns
- Compose column components into the board page
- Show the empty state for each column
