# Rename Columns

Add a column-specific editing workflow to show how a second feature owns its own
messages and behavior.

## Goal

Rename an existing board column.

## Outline

- Define the rename-column command message
- Handle column name changes
- Render the rename form
- Preserve the cards inside the renamed column
